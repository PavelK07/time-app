services:
  flask:
    image: pavelk07/time-app:v1.0
    container_name: flask_app

  nginx:
    image: nginx:latest
    container_name: nginx_proxy
    ports:
      - "38080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - flask